<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--Permiso para la lampara del celular-->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!--Permiso para usar la camara-->
    <uses-permission android:name="android.permission.CAMERA" />

    <!--Permiso para usar el internet-->
    <uses-permission android:name="android.permission.INTERNET"/>



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_app_scanner"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_scanner"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScannerCByQR">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity para códigos de barras -->
        <activity android:name=".ui.home.BarcodeResultActivity" />

        <!-- Activity para QR -->
        <activity android:name=".ui.home.QRResultActivity" />
    </application>

</manifest>
